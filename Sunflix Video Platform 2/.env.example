# MongoDB Configuration
# Copy this to `.env.local` for local development (DO NOT commit secrets)
VITE_MONGODB_URI=
VITE_API_URL=
VITE_DB_NAME=sunflix
JWT_SECRET=

# Copy this file to `.env.local` for local development (DO NOT commit secrets).
# In production, set these variables in your Vercel Project Settings > Environment Variables.
